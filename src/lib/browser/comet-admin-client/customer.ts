/*
import type { ICustomerInfo } from "$lib/types/customer.types";

export class Customer {
	constructor(private readonly client: any) {}

	async info() {
		const response = await this.client.get(`customer/info`);
		return response.data as ICustomerInfo;
	}


}

*/